tailwind.config = {
    theme: {
      extend: {
        // Heading Color 
        colors: {
          white: '#FFFFFF',
          iconColor: '#FFF84E',
          bgColor: '#000000',
        },
                // Heading Color End
        // container 

        spacing: {
          'headerContainer': '1450px',
        },

        // container End
        // banner images 

        backgroundImage: {
          'banner': "url('images/banner (2).png')",
        },

        // banner images End

        // Google Font 
        fontFamily: {
          'inter': ['Inter',],
        }
        // Google Font End
      }
    }
  }